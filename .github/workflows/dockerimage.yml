name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag smak/postgres-plv8:$(basename $(pwd)) --tag smak/postgres-plv8:$(date +%s)
